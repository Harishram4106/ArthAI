{
  "name": "arthai-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "vercel-build": "npx prisma generate --schema=apps/api/prisma/schema.prisma && npm run build -w apps/web"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
